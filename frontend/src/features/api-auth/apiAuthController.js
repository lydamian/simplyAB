const apiAuthController = {
  /**
   * Get an APIAuthToken for a userId.
   *
   * @param {Number} user_id
   */
  getAPIAuthToken: async (authToken) => {
    const APIAuthToken = 'auth-token';
    return APIAuthToken;
  },

  /**
   * Generates an API Auth Token.
   *
   * @param {Nunber} user_id
   */
  generateAPIAuthToken: async () => {
    const APIAuthToken = 'auth-token';
    return APIAuthToken;
  },
};

export default apiAuthController;
